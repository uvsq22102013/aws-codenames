{
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
    },
    "dependencies": {
        "@types/node": "^22.13.5",
        "axios": "^1.8.2",
        "react-google-recaptcha": "^3.1.0"
    }
}
